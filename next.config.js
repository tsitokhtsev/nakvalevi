/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: 'ahvmvkeajbesfkbymnhn.supabase.co',
            }
        ]
    }
}

module.exports = nextConfig
